<!DOCTYPE html>
<html>
  <head>
    <title>onedayoneline.com</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <!--tuboを一時的に無効化 deleteが機能しないから-->
    <%= javascript_include_tag 'application', 'data-turbo-track': 'reload', defer: true %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js"></script>
    <%= stylesheet_link_tag "application", media: 'all', "data-turbo-track": "reload" %>
    <!--これが全てのbootstrapのデザイン(navbar,form)を支えている。↓-->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <!-- <%= javascript_importmap_tags %> -->
  </head>
  <style>
  * {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}
.links ul {
  list-style-type: none; /* 点を消す */
  padding: 0; /* 左側の余白を削除 */
  margin: 0; /* 外側の余白を削除（必要に応じて） */
}


  </style>
  <body>
  <!--<div class="header d-flex flex-column justify-content-center align-items-center vh-100 text-center">--> 
  
 <!--User#showでは非表示にするコード-->
 <% if controller_name == 'users' && action_name == 'show' %>
 <div class="text-center">

  <button class="btn btn-light">
  <%= link_to "🏠", root_path, class: "text-dark text-decoration-none", data: { turbo: false } %>
  </button>

  <button class="btn btn-light">
    <%= link_to "✍️", zen_path(slug: 'a'), class: "text-dark text-decoration-none", data: { turbo: false } %>
  </button>

  <button class="btn btn-light">
   <%= link_to "👀", microposts_path, class: "text-dark text-decoration-none", data: { turbo: false } %>
  </button>
  <button class="btn btn-light">
   <%= link_to "📚", blog_path, class: "text-dark text-decoration-none", data: { turbo: false } %>
  </button>
  <button class="btn btn-light">
   <%= link_to "📱", app_path, class: "text-dark text-decoration-none", data: { turbo: false } %>
  </button>
  <button class="btn btn-light">
  <%= link_to "?", question_path, class: "text-dark text-decoration-none", data: { turbo: false } %>
  </button>
  </div>

 <% elsif controller_name == 'microposts' && action_name == 'zen_new' %>

<button class="btn btn-light">
  <%= link_to "🏠", root_path, class: "text-dark text-decoration-none", data: { turbo: false } %>
</button>
<button class="btn btn-light">
  <%= link_to "👤", user_path(slug: 'a'), class: "text-dark text-decoration-none", data: { turbo: false } %>
</button>

 <% else %>
 <div class="text-center mt-3" id="nav-section">
 <h1>
    <%= link_to "onedayoneline.com", root_path, class: "text-decoration-none text-dark" %>
  </h1>
  <p>言葉にするたび、英語があなたのものになる。</p>
  <p>hey, Kazuma!</p>
 <button class="btn btn-light mt-3 mb-3">
  <%= link_to "🏠", root_path, class: "text-dark text-decoration-none", data: { turbo: false } %>
  </button>
  <button class="btn btn-light">
  <%= link_to "👤", user_path(slug: 'a'), class: "text-dark text-decoration-none", data: { turbo: false } %>
  </button>
  <button class="btn btn-light mt-3 mb-3">
    <%= link_to "✍️", zen_path(slug: 'a'), class: "text-dark text-decoration-none", data: { turbo: false } %>
  </button>
  <button class="btn btn-light mt-3 mb-3">
   <%= link_to "👀", microposts_path, class: "text-dark text-decoration-none", data: { turbo: false } %>
  </button>
  <button class="btn btn-light mt-3 mb-3">
   <%= link_to "📖", blog_path, class: "text-dark text-decoration-none", data: { turbo: false } %>
  </button>
  <button class="btn btn-light mt-3 mb-3">
   <%= link_to "📱", app_path, class: "text-dark text-decoration-none", data: { turbo: false } %>
  </button>
  <button class="btn btn-light mt-3 mb-3">
  <%= link_to "?", question_path, class: "text-dark text-decoration-none", data: { turbo: false } %>
  </button>
</div>
 <% end %>




  <% flash.each do |message_type, message| %>
        <div class="alert alert-<%= message_type %>"><%= message %></div>
  <% end %>

  <%= yield %>
  
    <%# devise用↓ %>
    <% if notice %>
      <p><%= notice %></p>
    <% end %>
    <% if alert %>
      <p><%= alert %></p>
    <% end %>

<!--
<%= render 'layouts/footer' %>
<%= debug(params) if Rails.env.development? %>
-->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

  </body>
</html>
