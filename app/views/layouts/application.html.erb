<!DOCTYPE html>
<html>
  <head>
    <title>eigopencil</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- tuboを一時的に無効化 deleteが機能しないから -->
    <%= javascript_include_tag 'application', 'data-turbo-track': 'reload', defer: true %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js"></script>
    <%= stylesheet_link_tag "application", media: 'all', "data-turbo-track": "reload" %>
    <!-- これが全てのbootstrapのデザイン(navbar,form)を支えている。↓ -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <!-- <%= javascript_importmap_tags %> -->
    <%= favicon_link_tag "favicon.ico" %>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      .links ul {
        list-style-type: none; /* 点を消す */
        padding: 0; /* 左側の余白を削除 */
        margin: 0; /* 外側の余白を削除（必要に応じて） */
      }
      .diary-container {
        max-width: 600px;
        width: 100%;
        background: white;
        border-radius: 8px;
        padding: 20px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      }
      .diary-input {
        margin-bottom: 20px;
      }
      .diary-entries {
        margin-top: 20px;
      }
    .nav-section {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0.5rem 1rem; /* ナビゲーションバーの余白 */
}

.nav-section .logo {
  height: 35px; /* ロゴのサイズ調整 */
}

.nav-section .nav-links {
  flex: 1; /* ナビゲーションリンクを中央に寄せるための調整 */
  text-align: center;
}

.nav-section .spacer {
  flex: 1; /* 空のスペースでロゴとナビゲーションのバランスを取る */
}

.nav-section .nav-links button {
  margin: 0 0.25rem; /* 各ボタン間の余白を調整 */
}
</style>
  </head>

  <body>
<div class="d-flex align-items-center justify-content-between" id="nav-section">
   <!-- ロゴ -->
   <div class="logo-container ml-1">
   <%= link_to root_path, class: "logo-link" do %>
    <%= image_tag("logo01.png", class: "logo") %>
   <% end %>
  </div>
 
   <!-- ナビゲーションボタン -->
   <div class="nav-links text-center">
     <button class="btn btn-light p-3">
       <%= link_to "🏠", root_path, class: "text-dark text-decoration-none", data: { turbo: false } %>
     </button>
     <button class="btn btn-light p-3">
       <%= link_to "👤", current_user ? user_path(@user) : login_path, class: "text-dark text-decoration-none", data: { turbo: false } %>
     </button>
     <button class="btn btn-light p-3">
       <%= link_to "🧘‍♂️", current_user ? zen_path(@user) : login_path, class: "text-dark text-decoration-none", data: { turbo: false } %>
     </button>
     <button class="btn btn-light p-3">
       <%= link_to "📖", blog_path, class: "text-dark text-decoration-none", data: { turbo: false } %>
     </button>
     <button class="btn btn-light p-3">
       <%= link_to "?", question_path, class: "text-dark text-decoration-none", data: { turbo: false } %>
     </button>
   </div>
 
   <!-- 空スペース -->
   <div class="spacer"></div>
 </div>
 


    <% flash.each do |message_type, message| %>
      <div class="alert alert-<%= message_type %>"><%= message %></div>
    <% end %>

    <%= yield %>

    <%# devise用↓ %>
    <% if notice %>
      <p><%= notice %></p>
    <% end %>
    <% if alert %>
      <p><%= alert %></p>
    <% end %>

    <!--
    <%= render 'layouts/footer' %>
    <%= debug(params) if Rails.env.development? %>
    -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
